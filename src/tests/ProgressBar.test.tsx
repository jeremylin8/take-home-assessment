import { vi } from 'vitest'

import ProgressBar from '../components/ProgressBar'
import { render, screen } from './testing-lib'

import questions from '../questions.json'

describe('ProgressBar', () => {
    it(' progressBar should be proportion', () => {
        
    })
})